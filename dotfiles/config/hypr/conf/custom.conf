# Add your additional Hyprland configurations here

# Emoji picker — bemoji + wofi, copies pick to clipboard
bind = $mainMod, period, exec, BEMOJI_PICKER_CMD="wofi --dmenu --width=500 --height=400" bemoji
